fix: badge de sigla resolve por mapeamento_siglas (fonte real) + fallback sem-badge

A individual usava siglaPorNome (hardcoded, sem fallback -> null) e a geral usava
getSiglaCliente (hardcoded + fallback de INICIAIS). Divergiam: 11 clientes fora do
hardcoded ficavam sem badge na individual e com iniciais (potencialmente erradas)
na geral.

Novo helper siglaReal(nome, mapaFirestore): 1) SIGLA_PARA_NOME hardcoded;
2) mapeamento_siglas do Firestore (nome->sigla, carregado 1x em PoupancaVisao);
3) null -> SEM badge. NUNCA iniciais. PoupancaVisao carrega o mapa e passa as duas
views (tabela + detalhe) usarem siglaReal. getSiglaCliente mantido (ainda usado por
RankingEmpresariosModal), so nao mais nos badges de poupanca.

Remove a heuristica de iniciais que errava 6 de 11 (TIQUINHO TS->TQS, EDUARDO
EF->EFF, ALINE AAF->AAG, PEDRO GABRIEL PGF->PBF, GUILHERME GT->GGT) e inventava
sigla p/ quem nao tem (EDUARDA -> agora sem badge). Unifica individual e geral.

Display-only: typecheck ok, build ok, harness inalterado (-78.400 / check 0).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
