# Convenção — export da Visão Geral

Export (Excel/PDF) espelha a tabela — fonte única de colunas (`criarColunas` +
`valorColunaExport` em `features/visao-geral/columns.tsx`). Mesmas colunas visíveis,
mesma ordem, mesma visão (MC/EBITDA). Coluna oculta não sai; números crus na moeda.
