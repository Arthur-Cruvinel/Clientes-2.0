# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment variables (contêm credenciais Firebase)
.env.development
.env.production
.env.local
.env
*.local

# IDE
.vscode/*
!.vscode/extensions.json
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# TypeScript cache
*.tsbuildinfo

# Windows
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify
audit-results/

# Backups locais do Firestore — snapshots de segurança com DADO SENSÍVEL
# (alocação de clientes, folha, poupança). NUNCA versionar: o histórico git é
# permanente e o repo vive em OneDrive sem remoto privado adequado. Backups são
# segurança LOCAL apenas.
backups/firestore/

# Materiais de referência (modelos de proposta, guia de identidade) — não versionar
docs/Modelos/
